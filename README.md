# My-1st-Machine-Learning-project

A machine learning project that analyzes customer reviews and predicts their sentiment (e.g. *positive*, *neutral*, *negative*).  
This project demonstrates the full ML pipeline — from data cleaning and preprocessing to model training, evaluation, and reporting.

---

## 🧩 Features

- Cleans and preprocesses raw text data  
- Combines textual and numerical features using `ColumnTransformer`  
- Uses a complete `Pipeline` for training and prediction  
- Gradient Boosting model with class balancing  
- Evaluation with F1-score, classification report, and confusion matrix  

---

## 🛠️ Technologies Used

- **Python 3.11+**
- **pandas**
- **numpy**
- **scikit-learn**
- **re**, **string** (for regex-based text cleaning)

---
## 📦 Required Libraries

Make sure you have the following Python libraries installed:
```bash
pip install pandas numpy scikit-learn
